{/* <div className="text-center mb-[36px]">
        <h1 className="text-[60px] md:text-[70px] mt-[24px] text-pink font-lavishly">
          5A's
          <br />
          <span className="text-white">
            <span className="text-pink-500">Cake </span>&
          </span>
          <span className="text-pink-500"> Decoration</span>
        </h1>
        {/* <p className="text-[17px] text-gray-300 mt-3 max-w-4xl mx-auto font-courierNew">
          At 5A Decorations, we believe that every event is an opportunity to
          create unforgettable memories. From weddings and birthdays to
          corporate galas and intimate gatherings, we specialize in turning your
          vision into a spectacular reality
        </p>
        <button
          // onClick={() => navigate('/')}
          className="px-6 py-3 bg-pink-600 hover:bg-pink-700 text-white rounded-[24px] transition mt-2"
        >
          Contact Us
        </button> */}
      // </div>

      // <div
      //   className="relative max-w-7xl mx-auto group"
      //   onMouseEnter={stopAutoSlide}
      //   onMouseLeave={startAutoSlide}
      //   ref={containerRef}
      // >
      //   <div className="absolute left-2 top-1/2 -translate-y-1/2 z-40">
      //     <button
      //       onClick={handlePrev}
      //       className="bg-white/10 hover:bg-white/20 p-2 rounded-full"
      //     >
      //       <ChevronLeft size={30} />
      //     </button>
      //   </div>

      //   <div className="absolute right-2 top-1/2 -translate-y-1/2 z-40">
      //     <button
      //       onClick={handleNext}
      //       className="bg-white/10 hover:bg-white/20 p-2 rounded-full"
      //     >
      //       <ChevronRight size={30} />
      //     </button>
      //   </div>

      //   <div className="flex justify-center gap-6 transition-all duration-1000 ease-in-out px-4">
      //     {imageUrls.map((url, idx) => {
      //       const visibleIndexes = getVisibleIndexes();
      //       if (!visibleIndexes.includes(idx)) return null;

      //       return (
      //         <div
      //           key={idx}
      //           className={`w-full md:w-44 lg:w-72 h-100 md:h-80 lg:h-100 flex-shrink-0 ${getScaleAndStyle(
      //             idx
      //           )}`}
      //         >
      //           <img
      //             src={url}
      //             alt={`Event ${idx}`}
      //             className="w-full h-full object-cover rounded-lg shadow-xl"
      //           />
      //         </div>
      //       );
      //     })}
      //   </div>

      //   <div className="flex justify-center mt-[44px] space-x-2">
      //     {imageUrls.map((_, idx) => (
      //       <button
      //         key={idx}
      //         onClick={() => setCurrentIndex(idx)}
      //         className={`w-3 h-3 rounded-full transition-all duration-300 ${
      //           idx === currentIndex ? "bg-white scale-125" : "bg-gray-600"
      //         }`}
      //       />
      //     ))}
      //   </div>
        
      // </div>

      {/* <div className="relative max-w-4xl mx-auto overflow-hidden">
        <div className="relative w-full h-[400px] md:h-[500px] lg:h-[600px]">
          {imageUrls.map((url, idx) => (
            <img
              key={idx}
              src={url}
              alt="slide"
              className={`
              absolute top-0 left-0 w-full h-full object-cover 
              transition-opacity duration-1000 ease-in-out
              ${idx === currentIndex ? "opacity-100" : "opacity-0"}
            `}
            />
          ))}
        </div>

        <button
          onClick={handlePrev}
          className="absolute top-1/2 left-2 -translate-y-1/2 bg-white/20 hover:bg-white/30 p-2 rounded-full"
        >
          <ChevronLeft size={24} />
        </button>
        <button
          onClick={handleNext}
          className="absolute top-1/2 right-2 -translate-y-1/2 bg-white/20 hover:bg-white/30 p-2 rounded-full"
        >
          <ChevronRight size={24} />
        </button>

        <div className="flex justify-center mt-4 space-x-2">
          {imageUrls.map((_, idx) => (
            <span
              key={idx}
              className={`block w-3 h-3 rounded-full transition-transform ${
                idx === currentIndex ? "bg-white scale-125" : "bg-gray-600"
              }`}
            />
          ))}
        </div>
      </div> */} 
